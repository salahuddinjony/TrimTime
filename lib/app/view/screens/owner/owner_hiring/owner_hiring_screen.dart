import 'package:flutter/material.dart';

class OwnerHiringScreen extends StatelessWidget {
  const OwnerHiringScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('OwnerHiringScreen'),),
    );
  }
}
