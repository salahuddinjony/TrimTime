import 'package:flutter/material.dart';

class BarberQueScreen extends StatelessWidget {
  const BarberQueScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
